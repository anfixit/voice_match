# 🔐 Настройка GitHub Secrets для автоматического развертывания

## Пошаговая инструкция

### 1. Откройте репозиторий на GitHub

Перейдите по ссылке: https://github.com/anfixit/voice_match

### 2. Перейдите в настройки

Нажмите на **Settings** (Настройки) в верхнем меню репозитория

### 3. Откройте раздел Secrets

В левом боковом меню:
- Нажмите **Secrets and variables**
- Выберите **Actions**

### 4. Проверьте/добавьте секреты

Проверьте что у вас есть следующие секреты. Если каких-то нет - добавьте, нажав **New repository secret**:

#### Секрет 1: SSH_HOST
- **Name:** `SSH_HOST`
- **Value:** `109.73.194.190`
- Нажмите **Add secret**

#### Секрет 2: SSH_USER
- **Name:** `SSH_USER`
- **Value:** `root`
- Нажмите **Add secret**

#### Секрет 3: SSH_KEY
- **Name:** `SSH_KEY`
- **Value:** (ваш приватный SSH ключ)
- Нажмите **Add secret**

#### Секрет 4: SSH_PORT
- **Name:** `SSH_PORT`
- **Value:** `22` (или другой если нестандартный)
- Нажмите **Add secret**

#### Секрет 5: PROJECT_PATH
- **Name:** `PROJECT_PATH`
- **Value:** `/opt/voice-match`
- Нажмите **Add secret**

### 5. Проверьте секреты

После добавления всех секретов вы должны увидеть список:

```
SSH_HOST         *** (Updated X minutes ago)
SSH_USER         *** (Updated X minutes ago)
SSH_KEY          *** (Updated X minutes ago)
SSH_PORT         *** (Updated X minutes ago)
PROJECT_PATH     *** (Updated X minutes ago)
```

### 6. Готово!

Теперь при каждом push в ветку `main` GitHub Actions автоматически:
1. Подключится к вашему серверу
2. Обновит код
3. Пересоберет Docker контейнеры
4. Перезапустит приложение

## Визуальная схема

```
GitHub Repository
    └── Settings
        └── Secrets and variables
            └── Actions
                └── Секреты:
                    ├── SSH_HOST = 109.73.194.190
                    ├── SSH_USER = root
                    ├── SSH_KEY = (приватный ключ)
                    ├── SSH_PORT = 22
                    └── PROJECT_PATH = /opt/voice-match
```

## Тестирование

Чтобы протестировать автоматическое развертывание:

1. Внесите любое изменение в код
2. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "Test deployment"
   git push origin main
   ```
3. Перейдите в раздел **Actions** на GitHub
4. Вы увидите запущенный workflow "Deploy to Timeweb"
5. Нажмите на него чтобы увидеть прогресс

## Альтернативный вариант: Использование SSH ключа (более безопасно)

Вместо пароля можно использовать SSH ключ:

1. На сервере создайте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "github-actions"
   cat ~/.ssh/id_ed25519
   ```

2. Добавьте публичный ключ в authorized_keys:
   ```bash
   cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys
   ```

3. В GitHub Secrets:
   - Замените `SERVER_PASSWORD` на `SERVER_SSH_KEY`
   - В качестве значения используйте **приватный** ключ (содержимое `~/.ssh/id_ed25519`)

4. Обновите `.github/workflows/deploy.yml`:
   ```yaml
   - name: Deploy to server
     uses: appleboy/ssh-action@v1.0.0
     with:
       host: ${{ secrets.SERVER_HOST }}
       username: ${{ secrets.SERVER_USER }}
       key: ${{ secrets.SERVER_SSH_KEY }}  # вместо password
       port: 22
   ```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте пароли в репозиторий
- Используйте только GitHub Secrets для хранения чувствительных данных
- Секреты зашифрованы и не видны в логах GitHub Actions
- Рекомендуется использовать SSH ключи вместо паролей для production

## Поддержка

Если возникли проблемы:
1. Проверьте правильность введенных секретов
2. Убедитесь что сервер доступен: `ssh cg82264@185.114.245.123`
3. Проверьте логи в разделе **Actions** → конкретный workflow → детали выполнения
